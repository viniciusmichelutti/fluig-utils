{
  "name": "fluig-utils",
  "version": "1.0.0",
  "description": "Tools to use in fluig, using webservices",
  "keywords": [
    "fluig",
    "angular"
  ],
  "author": "Vinicius Michelutti",
  "license": "MIT",
  "dependencies": {
    "bower": "^1.7.9",
    "http-server": "^0.9.0"
  },
  "scripts": {
    "test": "echo \"No tests yet!\" && exit 1",
	
	"postinstall": "bower install",
	
	"prestart": "npm install",
	"start": "http-server -a 0.0.0.0 -p 8000 -c-1 ./app"
  }
}
